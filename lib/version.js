// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 23;
export const APP_LEVEL_NOTES = 'Fixes on Level 22. DUPLICATES: two reads of the same meter on the same day are now only flagged as a duplicate when the READING VALUE is identical (an accidental re-submission) — different-time, different-value reads are treated as legitimate. ROLLBACK ORDER: readings taken on the same date are now ordered by their actual start time, so a later reading no longer looks like a rollback of an earlier one (fixed false "reading went backwards" flags). EDIT FORM: the Meter dropdown now lists only the meters in the currently-selected VILLAGE. MINI-MAP: the reading-location pin now shows correctly (bundled Leaflet had a broken default marker icon). SUBMISSIONS: UID search now matches partial IDs. Fixed a blank "Reading corrected" box that appeared after a full-form edit. Meter maths unchanged.';
