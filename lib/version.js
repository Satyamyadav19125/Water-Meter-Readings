// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 26;
export const APP_LEVEL_NOTES = 'Fixes on Level 25. FORM DATA LAYOUT: a single reading now shows a small info card up top, then the photo(s) on the LEFT and the location mini-map + coordinates on the RIGHT, side by side, all on a clean white card. WHITE COMPARISON: the side-by-side duplicate table is now fully white — the light-blue difference rows and blue notice box are gone; differences are shown by bold text only. FIND TWICE-A-DAY READS: the "Read twice/day" tab (was "Duplicate") now lists EVERY meter read 2+ times on the same day regardless of value, so you can open each and edit or mark one dead manually — the automatic red flag stays stricter (identical value only) so it never cries wolf. Meter maths unchanged.';
