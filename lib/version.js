// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 20;
export const APP_LEVEL_NOTES = 'Big upgrade — brings the Water-Meter tool up to par with the PVC-Pipe tool. NEW: read-only Guest viewer (shareable link + its own landing name, capped real data, fake chat); turn farms or meters ON/OFF (disabled units greyed on the map, hidden from surveyors, dropped from targets and red flags); admin reading corrections + "mark as dead" that flow through analytics, flags, map and exports without touching Kobo; side-by-side duplicate review; faster canvas map with tappable pins, off/duplicate/flag filters and a Farm filter; chat unread badges + notifications; richer Excel export (All / Raw / Red flags / Duplicate / Dead / Corrected / Clean tabs + summary); smarter red flags (uses the reading DATE not upload time, "GPS far outside project area", review-window). Meter maths kept as-is: rollback / reverse / consumption stay meter-accurate. No existing feature removed.';
