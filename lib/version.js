// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 24;
export const APP_LEVEL_NOTES = 'Fixes on Level 23. LOOK: the Submissions detail + duplicate-comparison were recoloured to a calm, professional palette (the loud yellow/indigo "hospital" boxes and yellow difference rows are gone — differences are now a soft blue highlight). GPS: the reading-location panel now shows Latitude / Longitude / Altitude / Accuracy each with a copy button, plus a "Copy lat, lng" shortcut, and the comparison GPS row is copyable too. EDIT: the standalone "Correct the value" button is removed — you fix a reading by editing the whole form (the Reading field is in there), and "Mark as dead (mistake / duplicate)" now lives inside that same edit area (with Restore). TEAM: the Team tab now shows, per field assistant, how many readings they have actually taken (all-time + this period), distinct meters & villages, and when they were last active (flagged if quiet >10 days), plus a whole-team activity summary. Meter maths unchanged.';
