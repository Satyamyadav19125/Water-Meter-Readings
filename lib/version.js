// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 25;
export const APP_LEVEL_NOTES = 'Fixes on Level 24. GPS PANEL: removed the copy buttons — the reading-location block now just lists Latitude / Longitude / Altitude / Accuracy cleanly (values still selectable). COLORS: the expanded submission detail is now a clean white panel instead of being washed pink/red behind a red-flagged card, so the comparison table, photos and editor look professional; the comparison GPS row lost its copy button too. COMPARE: when two forms are shown side by side, the redundant "Edit full form" / "Mark as dead" pair at the bottom is hidden (each column already has its own Edit form / Correct / Mistake). TEAM COUNT FIX: "this week" per field assistant now counts readings by when they were SUBMITTED (upload time, matching "last active"), not the form date — surveyors often back-date the form, which made genuine this-week work show as 0. Meter maths unchanged.';
