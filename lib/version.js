// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 21;
export const APP_LEVEL_NOTES = 'Fixes + additions on top of Level 20. NEW: "➕ Take reading" buttons that open the Kobo form with village, meter, your name and today\'s date already filled; Google-Sheet meter-location sync + geofence red flag (Settings → Meter locations). FIXES: turned-off meters no longer appear in the Assignment/Missed tracker and are always tappable on the map (off/flagged pins drawn on top); in a duplicate comparison you can now Edit, Confirm-correct OR delete EACH reading independently (two reads on the same day at different times can both be kept), shown earliest-first; start/end times now shown on every submission; the Farms & meters on/off list now lets you toggle each meter one-by-one (flat list when the form has no farms) and the counts update instantly; empty "Farm ID" is hidden everywhere (UI + downloads) when your form has no farm question; registry/meter search fixed. Vercel photo edge-cache saver already included. Meter maths unchanged.';
