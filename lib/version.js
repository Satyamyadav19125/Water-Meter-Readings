// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 22;
export const APP_LEVEL_NOTES = 'Fixes on Level 21. MAP: Leaflet is now bundled with the app (no external CDN), so the map always loads even on blocked/slow networks — with a "Loading…" state and a Retry button if it ever fails. SUBMISSIONS: the detail no longer repeats Date / Reading / Surveyor (the old raw field dump is gone — Key facts + photo + location map remain); start/end times shown clearly. DUPLICATES: the "✎ Edit form" now opens the FULL form editor for EITHER reading, not just the value. RED FLAGS: "no reading for N days" is no longer a red flag — it lives in the Assignment / Missed-readings tab only. FARMS: the Farm filter and Farm columns are hidden everywhere when your Kobo form has no farm question. KOBO VIEW: added a searchable Submission ID column (search by UID) and dropped the always-empty Validation column. ASSIGNMENT: "➕ Take reading" and the status chips now line up. Meter maths unchanged.';
